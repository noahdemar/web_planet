node_modules/
dist/
.DS_Store
*.log
.vite/

# Generated by `npm run bake -- --write`. 48 MB at the default 1024, and a pure
# function of the seed in src/bake/plates.ts and the resolution — so it is
# reproducible in one command and there is no reason to version it. See the
# README: a fresh clone has to run the bake before `npm run dev` will start.
public/planet/
